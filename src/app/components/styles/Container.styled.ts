'use client'; 

import styled from 'styled-components'

export const Container = styled.div`
    right: 50%;
    bottom: 50%;
    transform: translate(50%, 50%);
    position: absolute;

    h1 {
        color: white;
    }
`